import { reduce } from "./DataReducer";

describe('AppReducer', () => {
    describe('reduce', () => {
        it('creates an initial state with no data', () => {
            expect(reduce()).toEqual({ data: {} });
        });

        it('')
    });
});